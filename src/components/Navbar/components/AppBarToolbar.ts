import { styled } from '@mui/material';
import MUIToolbar from '@mui/material/Toolbar';

const AppBarToolbar = styled(MUIToolbar)(() => ({
    justifyContent: 'center',
}));

export default AppBarToolbar;
