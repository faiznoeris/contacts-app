import { styled } from '@mui/material';
import MUIContainer from '@mui/material/Container';

const AppBarContainer = styled(MUIContainer)(() => ({
    padding: '0px 112px 0px 112px!important',
}));

export default AppBarContainer;
